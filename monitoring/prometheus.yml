global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "custom_exporter"
    static_configs:
      - targets: ["custom_exporter:8000"]
